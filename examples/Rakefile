all: hello build build/foo build/bar

hello:
	echo hello from Rakefile!

hello:
	echo hello from rakivo!

build:
	mkdir -p build

build/foo: src/foo.c
	cc -o $t $d

build/bar: src/bar.c src/bar.h
	cc -o $t $d[0]

.ALWAYS: hello
